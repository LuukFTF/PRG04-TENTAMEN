class Grid {
    // Fields
    private columns : number    = 12
    private rows    : number    = 7

    constructor() {
        console.log("Grid created!")

        for (let row = 0; row < this.rows; row++) {
            for (let column = 0; column < this.columns; column++) {
                // Voeg op deze plek een nieuw blokje toe aan het spel
            }
            
        }
    }
}